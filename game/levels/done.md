---
id: done
title: That's the tour
role: summary
---

You have now seen every item type and feature this engine offers: prose pages with each `role`, the three puzzle roles (`core`, `pretest`, `extra`), hints with a `when-goal`, prerequisites, remedies, an inventory, a gated check, and a multi-block prelude.

To make this game your own, edit `game/game.yaml` and the files under `game/levels/`. The engine's `docs/authoring.md` is the full reference.
